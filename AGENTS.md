# AGENTS.md

## 项目范围

本仓库用于管理内容创作、研究、提示词和相关工作过程。

## 工作规则

- 先阅读与当前任务直接相关的项目文档，再进行修改。
- 保持变更范围清晰，不无关改动现有内容。
- 新增内容放入职责匹配的目录：过程记录放入 `sessions/`，正式资料放入 `docs/`，提示词放入 `prompts/`。
- 敏感信息不得提交到仓库，包括 `.env`、secret、token、cookie、私钥和凭据文件。
- 提交信息使用简洁、明确的描述，说明本次变更的目的。

## 目录职责

- `sessions/`：短期、按次的工作记录和过程材料。
- `docs/`：长期维护、可供项目成员阅读的正式文档。
- `prompts/`：可复用、可版本管理的提示词和任务模板。

## 文档维护

- 项目状态更新写入 `PROJECT_STATUS.md`。
- 影响项目方向或长期维护方式的决定写入 `DECISIONS.md`。
